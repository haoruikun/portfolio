// const prototypes = document.querySelectorAll('.prototype');

// for (let i = 0; i < prototypes.length; i++ ) {
//     let previousElement = prototypes[i].parentElement.previousElementSibling.children[0];
//     prototypes[i].style.height = getComputedStyle(previousElement).getPropertyValue('height');
// }